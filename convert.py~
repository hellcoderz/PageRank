import fileinput
f = open("small.txt",'w+')
for line in fileinput.input("sample-small.txt"):
	strs = line.split(" ")
	f.write(strs[0]+"\t"+str(1))
	count = 0
	for st in strs:
		if count == 0:
			
		else:
			f.write("\t"+st)
		count = count + 1
	f.write("\n")
